/*
import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CreateRoutineDialogComponent } from './create-routine-dialog.component';

describe('CreateRoutineDialogComponent', () => {
  let component: CreateRoutineDialogComponent;
  let fixture: ComponentFixture<CreateRoutineDialogComponent>;

  beforeEach(() => {
    TestBed.configureTestingModule({
      declarations: [CreateRoutineDialogComponent]
    });
    fixture = TestBed.createComponent(CreateRoutineDialogComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/